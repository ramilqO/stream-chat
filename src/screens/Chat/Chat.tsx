import React, { FC } from 'react';
import { View, Text } from 'react-native';

const Chat: FC = () => {
    return (
        <View>
            <Text>Chat Component</Text>
        </View>
    );
};

export default Chat;
