import React, { FC } from 'react';
import { View, Text } from 'react-native';

const ChatList: FC = () => {
    return (
        <View>
            <Text>ChatList Component</Text>
        </View>
    );
};

export default ChatList;
