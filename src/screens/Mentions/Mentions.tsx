import React, { FC } from 'react';
import { View, Text } from 'react-native';

const Mentions: FC = () => {
    return (
        <View>
            <Text>Mentions Component</Text>
        </View>
    );
};

export default Mentions;
